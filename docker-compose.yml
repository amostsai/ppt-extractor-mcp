version: '3'

services:
  ppt_extractor_mcp:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./ppt_extractor_mcp:/app/ppt_extractor_mcp
    restart: unless-stopped
    environment:
      - LOG_LEVEL=INFO